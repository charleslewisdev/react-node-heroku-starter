import React from 'react';
import useStyles from './styles';

const SpacerColumn = () => {
  const styles = useStyles();

  return <div className={styles.SpacerColumn} />;
};

export default SpacerColumn;
